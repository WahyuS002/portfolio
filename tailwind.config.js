module.exports = {
    mode: 'jit',
    purge: ['./pages/*.html'],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {},
        fontFamily: {
            poppins: ['Poppins'],
        },
    },
    variants: {
        extend: {},
    },
    plugins: [],
}
